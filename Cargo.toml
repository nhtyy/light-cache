[package]
name = "light-cache"
version = "0.1.0"
edition = "2021"

[dependencies]
pin-project = "1.1.5"

[dev-dependencies]
tokio = { version = "1.39.2", features = ["rt", "rt-multi-thread", "macros", "time"] }